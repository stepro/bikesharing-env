name: azure/function-app
properties:
  appSettings:
    type: object
    propertyNames:
      pattern: "[a-zA-Z_][a-zA-Z0-9_]*"
    additionalProperties:
      type: string
exports:
  default:
    properties:
      endpoint:
        type: string
