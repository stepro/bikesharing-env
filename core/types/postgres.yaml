name: mongo
properties:
  schema:
    type: string
exports:
  default:
    usage:
      properties:
        roles:
          type: array
          items:
            type: string
    properties:
      connectionString:
        type: string
